# 🚭 금연 캘린더 - Firebase 설정 가이드

## 📋 빠른 시작

### 1단계: Firebase 프로젝트 만들기
1. [Firebase Console](https://console.firebase.google.com) 접속
2. **"프로젝트 추가"** 클릭
3. 프로젝트 이름 입력 (예: `no-smoking-calendar`)
4. Google Analytics → **"사용 안함"** 선택 후 생성

### 2단계: Firestore 데이터베이스 만들기
1. 좌측 메뉴 → **"Firestore Database"** 클릭
2. **"데이터베이스 만들기"** 클릭
3. ⚠️ **"테스트 모드에서 시작"** 선택 (중요!)
4. 위치: **asia-northeast3 (서울)** 선택

### 3단계: 웹 앱 등록 & Config 복사
1. 프로젝트 개요 화면 → 톱니바퀴 ⚙️ → **"프로젝트 설정"**
2. "내 앱" 섹션 → **웹 아이콘 `</>`** 클릭
3. 앱 닉네임 입력 → **"앱 등록"**
4. `firebaseConfig` 부분 복사

### 4단계: app.js에 Config 붙여넣기
`app.js` 파일 상단의 이 부분을 수정:

```javascript
const firebaseConfig = {
    apiKey: "여기에_복사한_값",
    authDomain: "여기에_복사한_값",
    projectId: "여기에_복사한_값",
    storageBucket: "여기에_복사한_값",
    messagingSenderId: "여기에_복사한_값",
    appId: "여기에_복사한_값"
};
```

### 5단계: 비밀번호 변경 (선택)
같은 `app.js` 파일에서 비밀번호 수정:

```javascript
USERS: {
    girlfriend: {
        password: 'love1234',  // ← 원하는 비밀번호로 변경
    },
    myungwoo: {
        password: 'cheer1234', // ← 원하는 비밀번호로 변경
    }
}
```

---

## 🌐 웹에 배포하기 (무료)

### 방법 1: GitHub Pages (추천)
1. GitHub에 저장소 생성
2. 3개 파일 업로드 (index.html, style.css, app.js)
3. Settings → Pages → Source: main branch
4. `https://username.github.io/repository-name` 으로 접속

### 방법 2: Firebase Hosting
```bash
npm install -g firebase-tools
firebase login
firebase init hosting
firebase deploy
```

### 방법 3: Netlify
1. [Netlify](https://netlify.com) 가입
2. 폴더를 드래그 앤 드롭
3. 자동 배포 완료!

---

## 🔒 보안 규칙 설정 (30일 후 필수)

테스트 모드는 30일 후 만료됩니다. Firebase Console에서:

**Firestore Database → 규칙** 탭에서:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /noSmokingData/{document=**} {
      // 누구나 읽기/쓰기 가능 (커플 전용이므로 OK)
      allow read, write: if true;
    }
  }
}
```

---

## ❓ 문제 해결

### "오프라인 모드" 표시됨
- `app.js`의 `firebaseConfig`가 제대로 입력되었는지 확인
- Firebase Console에서 Firestore가 활성화되었는지 확인

### 데이터가 동기화 안됨
- 브라우저 콘솔(F12)에서 에러 확인
- Firestore 보안 규칙이 "테스트 모드"인지 확인

### 로그인 안됨
- 비밀번호 확인 (기본값: girlfriend=`love1234`, myungwoo=`cheer1234`)

---

## 💕 사용법

| 계정 | 기본 비밀번호 | 할 수 있는 것 |
|------|--------------|---------------|
| 💕 여자친구 | love1234 | 금연 기록 작성, 메모 |
| 💪 명우 | cheer1234 | 도장 찍기, 응원 메시지 |

**실시간 동기화**: 한쪽에서 기록하면 다른 쪽에서 바로 보여요! 🎉
